import 'package:flutter/material.dart';

class NearbySharingPage extends StatelessWidget {
  const NearbySharingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Nearby Sharing')),
      body: const Center(child: Text('Nearby sharing UI coming soon')),
    );
  }
}
